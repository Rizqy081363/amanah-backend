/**
 * Centralized Pagination Architecture Constants
 * Per ARC-080 (Bounded collection reads), ARC-081 (Keyset pagination), and API-088 (Enforced server-side page size).
 */
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;
export const MIN_PAGE_SIZE = 1;
